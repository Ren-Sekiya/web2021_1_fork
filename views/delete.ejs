<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>データの削除</h2>
  <p>削除したい番号を押してください。</p>
  <br>
  <form action="/deletedata" method="post">
    <table>
      <tr>
        <td>登録番号&emsp;</td>
        <td>路線名&emsp;</td>
        <td>駅名&emsp;</td>
        <td>ゲームセンター名</td>
      </tr>
      <% for( let row of data ) { %>
        <tr>
            <td><input type="submit" value="<%=row.aid %>" id="aidbutton" name="number"></td>
            <td><%= row.name %>&emsp;</td>
            <td><%= row.name2 %>&emsp;</td>
            <td><a href="/public/<%= row.id %>"><%= row.name3 %></a></td>
        </tr>
      <% } %>
    </table>
  </form>
  <br>
  <form>
    <a href="/default">前のページに戻る</a>
  </form>
</body>
</html>