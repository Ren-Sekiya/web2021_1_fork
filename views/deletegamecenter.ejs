<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>削除したいゲームセンター名を入力してください。</h2>
  <form action="/deletegamecenter" name="insertform" method="post">
    ゲームセンター名：<input type="text" id="deletegamecenter" name="deletegamecenter">
    <input type="submit" value="削除" id="button" disabled>
    <script>
      function inputchange(event){
        let button = document.getElementById('button');
        button.disabled = false;
      }

      let text = document.getElementById('deletegamecenter');
      text.addEventListener('change', inputchange);
    </script>
  </form>
  <br>
  現在登録されているゲームセンター名↓
  <table>
      <% for( let row of data ) { %>
        <tr>
            <td><%= row.id %></td>
            <td><%= row.name %></td>
        </tr>
      <% } %>
  </table>
  <form action="/default">
    <a href="/public/delete.html">前のページに戻る</a>&emsp;
    <a href="/default">トップページに戻る</a>
  </form>
</body>
</html>